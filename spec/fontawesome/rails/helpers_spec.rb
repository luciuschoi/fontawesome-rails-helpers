# frozen_string_literal: true

RSpec.describe Fontawesome::Rails::Helpers do
  it "has a version number" do
    expect(Fontawesome::Rails::Helpers::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
